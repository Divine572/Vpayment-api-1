version: '3'
services:
  postgres:
    container_name: vpayment
    image: postgres:latest
    ports:
      - '4568:4568'
    volumes:
      - /data/postgres:/data/postgres
    env_file:
      - docker.env
    networks:
      - postgres

networks:
  postgres:
    driver: bridge
